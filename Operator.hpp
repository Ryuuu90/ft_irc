#ifndef OPERATOR_HPP
#define OPERATOR_HPP

#include "Client.hpp"


class Operator : public Client
{
    // public:
    //     // void    Invite(std::string nickName, std::string Channel);
    //     // void    Kick(std::string nickName, std::string Channel);
    //     // void    Mode();
	// 	Operator();
	// 	~Operator();
    //     // Operator& operator=(Client &client);
}; 

#endif
